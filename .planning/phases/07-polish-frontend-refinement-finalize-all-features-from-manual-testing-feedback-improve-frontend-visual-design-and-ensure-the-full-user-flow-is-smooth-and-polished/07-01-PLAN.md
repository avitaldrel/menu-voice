---
phase: 07-polish-frontend-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/Header.tsx
  - src/components/VoiceButton.tsx
  - src/components/ScanButton.tsx
  - src/components/MenuSummary.tsx
  - src/components/TranscriptDisplay.tsx
  - src/components/RetakeGuidance.tsx
  - src/components/ErrorState.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-13
  - D-14
  - D-15

must_haves:
  truths:
    - "App background is warm cream instead of stark white"
    - "Primary interactive buttons use teal/sage accent instead of black"
    - "Logo shows 'Menu' in near-black and 'Voice' in teal"
    - "Settings gear has 44px minimum tap target"
    - "Menu summary uses warm muted surfaces instead of gray"
    - "Transcript display uses warm muted background"
    - "Each screen state has one dominant hero button and subdued secondaries"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme color tokens in OKLCH"
      contains: "--color-accent"
    - path: "src/app/layout.tsx"
      provides: "Body with bg-background text-foreground"
      contains: "bg-background"
    - path: "src/components/Header.tsx"
      provides: "Teal logo, 44px gear tap target, updated aria-label"
      contains: "text-accent"
    - path: "src/components/ScanButton.tsx"
      provides: "Variant prop for primary (accent) and secondary (muted) styles"
      contains: "variant"
    - path: "src/components/VoiceButton.tsx"
      provides: "Accent-colored button states"
      contains: "bg-accent"
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "@theme CSS variables auto-generating Tailwind utility classes"
      pattern: "--color-accent.*oklch"
    - from: "src/app/layout.tsx"
      to: "globals.css"
      via: "bg-background utility class consuming --color-background variable"
      pattern: "bg-background"
---

<objective>
Warm palette design system and component color migration

Purpose: Replace the stark black/white/gray aesthetic with a warm, restaurant-friendly palette using teal/sage accent, warm cream backgrounds, and muted warm surfaces. This is the visual foundation that all subsequent plans build upon.

Output: Updated globals.css with @theme tokens, layout.tsx body migration, and all 7 UI components updated to use the new palette classes.
</objective>

<execution_context>
@.planning/phases/07-polish-frontend-refinement-finalize-all-features-from-manual-testing-feedback-improve-frontend-visual-design-and-ensure-the-full-user-flow-is-smooth-and-polished/07-UI-SPEC.md
@.planning/phases/07-polish-frontend-refinement-finalize-all-features-from-manual-testing-feedback-improve-frontend-visual-design-and-ensure-the-full-user-flow-is-smooth-and-polished/07-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/app/layout.tsx
@src/components/Header.tsx
@src/components/VoiceButton.tsx
@src/components/ScanButton.tsx
@src/components/MenuSummary.tsx
@src/components/TranscriptDisplay.tsx
@src/components/RetakeGuidance.tsx
@src/components/ErrorState.tsx
@src/app/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/voice-state.ts:
```typescript
export type VoiceState =
  | { status: 'idle' }
  | { status: 'listening'; transcript: string }
  | { status: 'processing'; transcript: string }
  | { status: 'speaking'; transcript: string; response: string }
  | { status: 'error'; message: string };
```

From src/components/ScanButton.tsx:
```typescript
interface ScanButtonProps {
  onFilesSelected: (files: File[]) => void;
  disabled?: boolean;
}
```

From src/components/VoiceButton.tsx:
```typescript
interface VoiceButtonProps {
  status: VoiceState['status'];
  onTap: () => void;
  disabled?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens, layout body, and Header updates</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/components/Header.tsx</files>
  <read_first>src/app/globals.css, src/app/layout.tsx, src/components/Header.tsx, src/app/__tests__/layout.test.tsx</read_first>
  <action>
**globals.css** — Replace the entire file with Tailwind v4 @theme design tokens per UI-SPEC Color section and RESEARCH.md Pattern 1.

Replace the existing content with:
```css
@import "tailwindcss";

@theme {
  /* Dominant surface — warm off-white cream (D-02) */
  --color-background: oklch(97% 0.015 85);

  /* Primary foreground — warm near-black */
  --color-foreground: oklch(22% 0.01 85);

  /* Secondary surface — warm light muted (cards, panels, nav) */
  --color-muted: oklch(93% 0.01 85);
  --color-muted-foreground: oklch(50% 0.01 85);

  /* Accent — teal/sage for primary interactive elements (D-01) */
  --color-accent: oklch(55% 0.12 185);
  --color-accent-hover: oklch(48% 0.12 185);
  --color-accent-foreground: oklch(99% 0 0);

  /* Destructive — muted warning red */
  --color-destructive: oklch(55% 0.18 25);
  --color-destructive-foreground: oklch(99% 0 0);

  /* Warning surface — retake/extraction warnings */
  --color-warning-surface: oklch(95% 0.05 85);
  --color-warning-border: oklch(80% 0.10 85);
  --color-warning-text: oklch(40% 0.08 65);
}

body {
  background: var(--color-background);
  color: var(--color-foreground);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

/* WCAG 2.3.3 — disable all motion for users who prefer reduced motion */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
  }
}
```

Remove the `:root` hex variables block, the `@theme inline` block, and the `@media (prefers-color-scheme: dark)` block (dark mode deferred to v2 per UI-SPEC).

**layout.tsx** — On the `<body>` element, replace `className="min-h-screen bg-white text-black antialiased"` with `className="min-h-screen bg-background text-foreground antialiased"`. Per D-02 and RESEARCH.md Pitfall 2, this picks up the warm cream from the CSS variable.

Also update the skip link: replace `focus:bg-black focus:text-white` with `focus:bg-accent focus:text-accent-foreground` and replace `focus:rounded` with `focus:rounded-lg`.

**Header.tsx** — Make these specific changes (per D-03, D-13, UI-SPEC Header section):
1. Replace `bg-white` with `bg-background` in the header className
2. Replace `border-gray-100` with `border-muted` in the border class
3. Replace the logo span from `Menu<span className="text-gray-400">Voice</span>` to `Menu<span className="text-accent">Voice</span>` — teal "Voice" per D-03
4. On the settings `<Link>`, replace `aria-label="Settings"` with `aria-label="Open Settings — manage allergies and preferences"` per D-13
5. On the settings `<Link>`, replace `className="p-2 rounded-lg ..."` — change `p-2` to `p-2.5` to increase tap target from 40px to 44px minimum per UI-SPEC
6. Replace `focus-visible:outline-black` with `focus-visible:outline-accent` on the settings Link
7. After the SVG icon, add a visible label for wider viewports: `<span className="sr-only sm:not-sr-only sm:text-sm sm:ml-1">Settings</span>` per UI-SPEC

Update the layout test if it has assertions on specific class names or aria-label text that would break.
  </action>
  <verify>
    <automated>cd "C:/Users/2fire/All Coding/menu voice" && npm test -- --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-accent: oklch(55% 0.12 185)` (grep-verifiable)
    - globals.css contains `--color-background: oklch(97% 0.015 85)` (grep-verifiable)
    - globals.css contains `--color-muted: oklch(93% 0.01 85)` (grep-verifiable)
    - globals.css contains `--color-warning-surface: oklch(95% 0.05 85)` (grep-verifiable)
    - globals.css contains `prefers-reduced-motion: reduce` media query (grep-verifiable)
    - globals.css does NOT contain `#fafafa` or `#171717` (grep-verifiable)
    - layout.tsx contains `bg-background text-foreground` on body element (grep-verifiable)
    - layout.tsx does NOT contain `bg-white text-black` (grep-verifiable)
    - Header.tsx contains `text-accent` on the "Voice" span (grep-verifiable)
    - Header.tsx contains `p-2.5` on settings Link (grep-verifiable)
    - Header.tsx contains `aria-label="Open Settings — manage allergies and preferences"` (grep-verifiable)
    - Header.tsx contains `bg-background` instead of `bg-white` (grep-verifiable)
    - Header.tsx contains `sm:not-sr-only` visible Settings label span (grep-verifiable)
    - `npm test` passes (all existing tests green)
  </acceptance_criteria>
  <done>globals.css has full @theme token block with OKLCH colors, prefers-reduced-motion rule, no legacy hex vars. layout.tsx body uses bg-background text-foreground. Header has teal logo, 44px gear target, descriptive aria-label, and visible Settings label on wider viewports. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Component color migration — all UI components to warm palette</name>
  <files>src/components/VoiceButton.tsx, src/components/ScanButton.tsx, src/components/MenuSummary.tsx, src/components/TranscriptDisplay.tsx, src/components/RetakeGuidance.tsx, src/components/ErrorState.tsx, src/app/page.tsx</files>
  <read_first>src/components/VoiceButton.tsx, src/components/ScanButton.tsx, src/components/MenuSummary.tsx, src/components/TranscriptDisplay.tsx, src/components/RetakeGuidance.tsx, src/components/ErrorState.tsx, src/app/page.tsx, src/components/__tests__/VoiceButton.test.tsx, src/components/__tests__/ScanButton.test.tsx, src/components/__tests__/MenuSummary.test.tsx, src/components/__tests__/RetakeGuidance.test.tsx, src/components/__tests__/ErrorState.test.tsx</read_first>
  <action>
**VoiceButton.tsx** — Update `getButtonClasses` per UI-SPEC VoiceButton section:
- Replace `focus-visible:outline-black` with `focus-visible:outline-accent` in the `base` string
- `idle`: replace `bg-black text-white` with `bg-accent text-accent-foreground`
- `listening`: replace `bg-black text-white ring-4 ring-blue-600 ring-offset-2 animate-pulse` with `bg-accent text-accent-foreground ring-4 ring-accent ring-offset-2 animate-pulse`
- `processing`: replace `bg-gray-400 text-white` with `bg-muted text-muted-foreground`
- `speaking`: replace `bg-gray-400 text-white` with `bg-muted text-muted-foreground`
- `error`: replace `bg-gray-200 text-gray-600` with `bg-muted text-muted-foreground/60`

**ScanButton.tsx** — Add `variant` prop per UI-SPEC ScanButton section and D-04:
1. Add `variant?: 'primary' | 'secondary'` and `label?: string` to `ScanButtonProps` interface (defaults: `variant = 'primary'`, `label = 'Scan Menu'`)
2. For `primary` variant: replace `bg-black text-white` with `bg-accent text-accent-foreground`, replace `active:bg-gray-800` with `active:bg-accent-hover`, replace `focus-visible:outline-black` with `focus-visible:outline-accent`
3. For `secondary` variant: use `bg-muted text-foreground border border-muted-foreground/30 min-h-[48px] text-base font-normal` (smaller, subdued per D-04). Remove `shadow-lg hover:shadow-xl` for secondary.
4. The button label displays `{label}` instead of hardcoded "Scan Menu"
5. Update aria-label to use the `label` prop value: `aria-label={\`${label} — tap to photograph a restaurant menu\`}`

**page.tsx** — Update the Welcome button and Results scan section per UI-SPEC:
1. Welcome button: replace `bg-black text-white` with `bg-accent text-accent-foreground`, replace `active:bg-gray-800` with `active:bg-accent-hover`, replace `focus-visible:outline-black` with `focus-visible:outline-accent`. Change button text from "Start" to "Scan My Menu" per UI-SPEC Copywriting. Update the `aria-label` to "Scan My Menu — tap to begin"
2. Results footer ScanButton: add `variant="secondary"` and `label="Scan New Menu"` props to the ScanButton in the results section border-t div (per D-12)
3. Replace `border-gray-200` in the results footer with `border-muted`
4. In the idle state, replace `text-gray-500` with `text-muted-foreground`
5. ErrorState retry button: update ErrorState.tsx — replace `bg-black text-white` with `bg-accent text-accent-foreground`, replace `active:bg-gray-800` with `active:bg-accent-hover`, replace `focus-visible:outline-black` with `focus-visible:outline-accent`

**MenuSummary.tsx** — Update per UI-SPEC MenuSummary section:
1. Replace `text-gray-600` with `text-muted-foreground` on the menu type paragraph
2. Replace `text-gray-500` with `text-muted-foreground` on the category/item count paragraph
3. Warning div: replace `bg-yellow-50 border-yellow-200` with `bg-warning-surface border-warning-border`, replace `text-yellow-800` with `text-warning-text`, replace `text-yellow-700` with `text-warning-text`
4. CategorySection button: replace `bg-gray-50 hover:bg-gray-100` with `bg-muted hover:bg-muted/80`, replace `focus-visible:outline-black` with `focus-visible:outline-accent`
5. Category border: replace `border-gray-200` with `border-muted`
6. CategorySection item count span: replace `text-gray-500` with `text-muted-foreground`
7. Item price: replace `text-gray-700` with `text-foreground`
8. Item description: replace `text-gray-600` with `text-muted-foreground`
9. Dietary flag badges: replace `bg-green-100 text-green-800` with `bg-accent/15 text-accent`
10. Expanded list: replace `divide-gray-100` with `divide-muted`

**TranscriptDisplay.tsx** — Update per UI-SPEC TranscriptDisplay section:
1. Container: replace `bg-gray-50 rounded-lg` with `bg-muted rounded-xl`
2. User transcript: replace `text-gray-900` with `text-foreground`
3. Assistant response: replace `text-gray-700` with `text-foreground`

**RetakeGuidance.tsx** — Update VISUAL CLASSES ONLY (TTS fix is in Plan 02):
1. Guidance panel: replace `text-yellow-800 bg-yellow-50` with `text-warning-text bg-warning-surface border border-warning-border`
2. Proceed button: replace `bg-gray-700 text-white` with `bg-muted text-foreground`, replace `focus-visible:outline-black` with `focus-visible:outline-accent`

Update any test files that assert on specific class names (e.g., `bg-black`, `bg-gray-50`, `text-gray-700`) to use the new class names.
  </action>
  <verify>
    <automated>cd "C:/Users/2fire/All Coding/menu voice" && npm test -- --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - VoiceButton.tsx contains `bg-accent text-accent-foreground` for idle state (grep-verifiable)
    - VoiceButton.tsx contains `ring-accent` for listening state (grep-verifiable)
    - VoiceButton.tsx does NOT contain `bg-black` or `ring-blue-600` (grep-verifiable)
    - ScanButton.tsx contains `variant` in the interface definition (grep-verifiable)
    - ScanButton.tsx contains `bg-accent text-accent-foreground` for primary (grep-verifiable)
    - ScanButton.tsx contains `bg-muted text-foreground` for secondary (grep-verifiable)
    - page.tsx Welcome button contains `bg-accent text-accent-foreground` (grep-verifiable)
    - page.tsx Welcome button text is "Scan My Menu" (grep-verifiable)
    - page.tsx results ScanButton has `variant="secondary"` and `label="Scan New Menu"` (grep-verifiable)
    - MenuSummary.tsx contains `bg-warning-surface` for warnings (grep-verifiable)
    - MenuSummary.tsx contains `bg-accent/15 text-accent` for dietary badges (grep-verifiable)
    - MenuSummary.tsx does NOT contain `bg-green-100` or `bg-yellow-50` (grep-verifiable)
    - TranscriptDisplay.tsx contains `bg-muted rounded-xl` (grep-verifiable)
    - TranscriptDisplay.tsx does NOT contain `bg-gray-50` or `text-gray-700` (grep-verifiable)
    - RetakeGuidance.tsx contains `bg-warning-surface` (grep-verifiable)
    - ErrorState.tsx contains `bg-accent text-accent-foreground` (grep-verifiable)
    - ErrorState.tsx does NOT contain `bg-black` (grep-verifiable)
    - `npm test` passes (all existing tests green)
  </acceptance_criteria>
  <done>All 7 UI components and page.tsx use warm palette tokens (accent, muted, warning-surface). No remaining gray-50, gray-100, gray-200, bg-black, yellow-50, green-100, or bg-gray-700 in component files. ScanButton has variant prop. Welcome button says "Scan My Menu" with accent colors. Results footer has "Scan New Menu" secondary variant. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm test` — all tests pass
- grep for `bg-black` in src/components/ returns zero matches (all migrated to accent)
- grep for `bg-gray-50` in src/components/ returns zero matches (all migrated to muted)
- grep for `bg-white` in src/app/layout.tsx returns zero matches (migrated to bg-background)
- grep for `#fafafa` or `#171717` in src/app/globals.css returns zero matches (replaced with OKLCH)
- grep for `bg-yellow-50` in src/components/ returns zero matches (migrated to warning-surface)
</verification>

<success_criteria>
1. Opening the app shows warm cream background, not white
2. All primary buttons are teal, not black
3. Menu summary uses warm surfaces and teal dietary badges
4. Header logo has teal "Voice" text
5. Settings gear has 44px tap target and descriptive aria-label
6. "Scan New Menu" secondary button visible in results state
7. All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-frontend-refinement-finalize-all-features-from-manual-testing-feedback-improve-frontend-visual-design-and-ensure-the-full-user-flow-is-smooth-and-polished/07-01-SUMMARY.md`
</output>
