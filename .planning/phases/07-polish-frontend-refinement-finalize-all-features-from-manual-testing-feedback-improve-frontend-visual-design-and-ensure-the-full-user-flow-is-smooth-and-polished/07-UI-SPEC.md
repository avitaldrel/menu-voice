---
phase: 7
slug: polish-frontend-refinement
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-31
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for Phase 7: Polish & Frontend Refinement.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind v4 CSS-first, manual design tokens |
| Preset | not applicable |
| Component library | none (custom components, React 19 + Tailwind v4) |
| Icon library | inline SVGs only — no external icon library |
| Font | system stack: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif` |

**Token location:** All design tokens live in `src/app/globals.css` inside `@theme` blocks. No `tailwind.config.js` — Tailwind v4 is CSS-first. All palette changes go in `globals.css` only.

**Source:** RESEARCH.md Pattern 1 — Tailwind v4 `@theme` system.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline tight spacing |
| sm | 8px | Button internal padding, compact list gaps |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, vertical gaps between UI zones |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Hero button vertical padding, major section breaks |
| 3xl | 64px | Page-level top/bottom breathing room |

Exceptions:
- Touch targets: minimum 48px height on all interactive elements (A11Y-03 hard requirement — current `min-h-[72px]` on CTA buttons is correct and must be preserved)
- VoiceButton: 96px diameter (w-24 h-24 = 96px) — established in Phase 2, do not reduce
- Settings gear icon tap target: minimum 44px (upgrade from current 24px icon + 8px padding = 40px; pad to 44px minimum for D-13)

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 (regular) | 1.5 | TranscriptDisplay text, category item descriptions, guidance copy |
| Label | 14px | 400 (regular) | 1.4 | Metadata (item count, menu type label, status indicator text) |
| Heading | 24px | 600 (semibold) | 1.2 | Restaurant name (MenuSummary h2), category headers, state headings |
| Display | 32px | 700 (bold) | 1.1 | Welcome "Start" button label, hero action labels |

**Constraints:**
- Exactly 2 weights: 400 (regular) and 600/700 (semibold/bold). Bold (700) is reserved for the single hero CTA per screen only (D-04 one hero action per screen).
- Body size must stay at 16px minimum — accessibility requirement for low-vision users (A11Y-03).
- No 12px or 13px text anywhere in the UI — smallest permitted size is 14px (label role).

---

## Color

### Design Token Definitions (globals.css `@theme` block)

```css
@theme {
  /* Dominant surface — warm off-white cream (D-02) */
  --color-background: oklch(97% 0.015 85);

  /* Primary foreground — warm near-black */
  --color-foreground: oklch(22% 0.01 85);

  /* Secondary surface — warm light muted (cards, panels, nav) */
  --color-muted: oklch(93% 0.01 85);
  --color-muted-foreground: oklch(50% 0.01 85);

  /* Accent — teal/sage for primary interactive elements (D-01) */
  --color-accent: oklch(55% 0.12 185);
  --color-accent-hover: oklch(48% 0.12 185);
  --color-accent-foreground: oklch(99% 0 0);

  /* Destructive — muted warning red for retake guidance panels */
  --color-destructive: oklch(55% 0.18 25);
  --color-destructive-foreground: oklch(99% 0 0);

  /* Warning surface — retake guidance, extraction warnings */
  --color-warning-surface: oklch(95% 0.05 85);
  --color-warning-border: oklch(80% 0.10 85);
  --color-warning-text: oklch(40% 0.08 65);
}
```

**Source:** RESEARCH.md Pattern 1 — OKLCH values directly from research; hue 85 = warm yellow-orange, hue 185 = teal-cyan, hue 25 = warm red.

### 60/30/10 Color Contract

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `--color-background` | `oklch(97% 0.015 85)` — warm cream | Page background, body, all screen surfaces |
| Secondary (30%) | `--color-muted` | `oklch(93% 0.01 85)` — warm light gray | TranscriptDisplay container, category expand panels, header background, card surfaces |
| Accent (10%) | `--color-accent` | `oklch(55% 0.12 185)` — teal/sage | Reserved-for list below |
| Destructive | `--color-destructive` | `oklch(55% 0.18 25)` — muted red | Retake flow "Scan New Menu" after 2+ failed attempts only |

**Accent reserved for (explicit list):**
1. VoiceButton background in `idle` state — the one dominant interactive element on the results screen
2. ScanButton background — the one dominant interactive element on the idle/welcome screen
3. Welcome "Start" button background
4. Header "MenuVoice" logo teal portion (updates D-03 text logo to use accent teal on "Voice")
5. Focus ring color on all focusable elements (replaces current `focus-visible:outline-black`)
6. VoiceButton `listening` ring — replaces current `ring-blue-600` with `ring-accent`

**Accent is NOT used for:** secondary actions, metadata text, expanded category chevrons, disabled states, borders, dividers, or background surfaces.

### Body Background Migration

`layout.tsx` currently has `bg-white` hardcoded on `<body>`. This must be replaced with `bg-background text-foreground` to pick up the `--color-background` CSS variable (RESEARCH.md Pitfall 2).

### Dark Mode

Defer to v2. Phase 7 targets light mode only. The existing `@media (prefers-color-scheme: dark)` block in globals.css is left unchanged but does not receive the new warm palette.

---

## Component-Level Visual Contracts

### Welcome Screen ("Start" button)

- Background: `bg-accent` (teal)
- Text: `text-accent-foreground` (white)
- Size: `w-full max-w-md min-h-[200px]` — existing size preserved
- Weight: 700 bold, 32px Display role
- Active state: `active:bg-accent-hover` (darker teal, replaces `active:bg-gray-800`)
- Border radius: `rounded-3xl` — existing, preserved
- Source: CONTEXT.md D-04 one hero action; D-01 teal/sage accent

### ScanButton (idle + results footer)

- **Primary instance (idle state):** `bg-accent text-accent-foreground` — full-width hero button, min-h-[72px], text 24px semibold
- **Secondary instance (results footer):** Labeled "Scan New Menu" (D-12). Subdued style: `bg-muted text-foreground border border-muted-foreground/30` — smaller, not competing with VoiceButton. Min-h-[48px]. Text 16px regular. (Source: D-04 one hero per screen — VoiceButton is the hero in results state)
- Source: CONTEXT.md D-04, D-12; RESEARCH.md Pattern 7

### VoiceButton (results state)

- `idle`: `bg-accent text-accent-foreground` — teal, 96px circle (hero button)
- `listening`: `bg-accent text-accent-foreground ring-4 ring-accent ring-offset-2 animate-pulse` — teal ring replaces blue
- `processing`: `bg-muted text-muted-foreground` — warm muted, not black/gray-400
- `speaking`: `bg-muted text-muted-foreground` — same as processing (subdued, non-interactive feel)
- `error`: `bg-muted text-muted-foreground/60` — even more subdued
- Focus ring: `focus-visible:outline-accent` (teal, replaces current `outline-black`)
- Source: CONTEXT.md D-01, D-04; codebase VoiceButton.tsx

### Header

- Background: `bg-background` (warm cream, not white) — aligns with page surface
- Border: `border-muted` — warm neutral divider
- Logo: `Menu` in `text-foreground` (near-black) + `Voice` in `text-accent` (teal) — implements D-03
- Settings gear icon: minimum 44px tap target — enlarge hit area from current 40px to 44px via `p-2.5` (10px padding + 24px icon = 44px). Keep `aria-label="Settings"` as-is.
- Source: CONTEXT.md D-03, D-13

### ProcessingState (extraction progress)

- Background: `bg-background` (inherits, no separate surface)
- Message text: 16px body, weight 400, `text-foreground`
- Cycling messages (visual only, ARIA region stays static):
  1. "Reading your menu..."
  2. "Finding categories..."
  3. "Almost done..."
  4. "Just a moment..."
- Message interval: 4000ms per cycle
- Transition: `transition-opacity duration-300 ease-in-out` between messages
- Spinner/pulse: `animate-pulse` on message text only — not a loading spinner graphic
- ARIA live region: static, shows only the initial message from page.tsx — never cycles (RESEARCH.md Pattern 3 constraint)
- Source: CONTEXT.md D-08; RESEARCH.md Pattern 3

### TranscriptDisplay

- Container: `bg-muted rounded-xl p-4` (warm muted surface, replaces current `bg-gray-50`)
- User turn: `font-semibold text-foreground` label "You:" + regular weight transcript text
- Assistant turn: `text-foreground` (same foreground, not gray-700)
- Max height: `max-h-48 overflow-y-auto` — existing, preserved
- Source: CONTEXT.md D-05

### MenuSummary (results state, D-06 — Claude's Discretion)

Visual display style decision: **Collapsible accordion panels with warm muted surfaces.** The current expand/collapse pattern is retained as it works well voice-first (sighted companion can tap to expand; blind users get all data via voice). Updates:
- Category button: `bg-muted hover:bg-muted/80` (warm tone, replaces `bg-gray-50 hover:bg-gray-100`)
- Category border: `border-muted` (warm, replaces `border-gray-200`)
- Dietary flag badges: `bg-accent/15 text-accent` (teal tint, replaces `bg-green-100 text-green-800`) — uses accent at low opacity as a surface, stays within 10% budget
- Warning panel: `bg-warning-surface border-warning-border text-warning-text` (warm amber, replaces `bg-yellow-50 border-yellow-200`)
- The MenuSummary remains visible during voice conversation — no collapse at results state entry (D-15: menu data stays accessible)
- Source: CONTEXT.md D-06 (discretion), D-15

### RetakeGuidance

- Guidance panel: `bg-warning-surface border border-warning-border text-warning-text rounded-xl p-4` (warm amber, replaces current `bg-yellow-50 text-yellow-800`)
- Retake ScanButton: accent style (hero button, full-width)
- Proceed button (2+ attempts): `bg-muted text-foreground` — subdued secondary style (not `bg-gray-700`)
- Source: CONTEXT.md D-14; codebase RetakeGuidance.tsx

### Settings Icon (gear) discoverability

- Keep existing SVG gear icon
- Enlarge tap target: `p-2.5` (replaces `p-2`) — tap area becomes 44px minimum
- Add visible label text next to gear on wider viewports: `<span className="sr-only sm:not-sr-only sm:text-sm">Settings</span>` — visible on tablet+, screen-reader announced on mobile
- Update `aria-label` to: `"Open Settings — manage allergies and preferences"` (more descriptive for D-13 VoiceOver discoverability)
- Source: CONTEXT.md D-13

---

## State Transitions

**Source:** CONTEXT.md D-07, D-09, D-10; RESEARCH.md Pattern 2.

### Fade-In Wrapper (FadePanel component)

All major state panels (welcome, idle, processing, results, retake, error) wrap their content in a `FadePanel` component:

```
mount → opacity-0 → requestAnimationFrame → opacity-100
duration: 300ms
easing: ease-in-out
Tailwind classes: transition-opacity duration-300 ease-in-out
```

- Error and retake states use `duration-200` (faster — avoids artifact on quick retry, RESEARCH.md Pitfall 3)
- Welcome → idle transition uses `duration-400` (success path, more deliberate)
- Results state uses `duration-300`

### Fade-Out Before State Dispatch

For transitions out of a state (before dispatch), delay the reducer action by 150ms to allow opacity-0 to complete:

```
user action → set opacity-0 → setTimeout 150ms → dispatch reducer action → new state mounts with fade-in
```

Only applied to intentional user-initiated transitions (Start tap, Scan tap, Retry tap). Not applied to automatic transitions (extract success → results) — those use fade-in on arrival only.

### prefers-reduced-motion

All transitions disabled when `prefers-reduced-motion: reduce`:

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
  }
}
```

Add to `globals.css`. This satisfies WCAG 2.3.3 (W3C C39 technique). ARIA live regions are never animated (RESEARCH.md Pitfall 4).

### TTS Fade-Out for Interrupts (D-10)

When user taps to interrupt speaking:
- `audioElement.volume` ramps from current volume to 0 over 200ms (10 steps × 20ms)
- After fade completes: `audio.pause()`, reset `audio.volume = 1`
- Brief 300ms pause (existing `setTimeout` in `handleScreenTap`)
- Then `startListening()` activates

This happens entirely within `TTSClient.stop()`. Does NOT use SpeechSynthesis (CLAUDE.md hard constraint).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — Welcome | "Start" |
| Primary CTA — Idle (scan) | "Scan Menu" |
| Primary CTA — Results (mic) | aria-label: "Tap to start listening" (existing, preserved) |
| Secondary CTA — Scan New Menu | "Scan New Menu" |
| Primary CTA — Retake | "Scan Menu" (ScanButton label, existing) |
| Primary CTA — Retake secondary | "Proceed with what I have" (existing, preserved) |
| Empty state heading — Idle | "Take a photo of your menu" |
| Empty state body — Idle | "Point your camera at the menu and snap a picture" |
| Empty state — No transcript | (no text — TranscriptDisplay returns null, existing behavior) |
| Processing message 1 | "Reading your menu..." |
| Processing message 2 | "Finding categories..." |
| Processing message 3 | "Almost done..." |
| Processing message 4 | "Just a moment..." |
| Processing TTS periodic reminder | "Still reading your menu, one moment please." — routed through TTSClient (fixes D-11/D-03 SpeechSynthesis bug) |
| Welcome TTS | "Welcome to MenuVoice. Please take a photo of the menu." — routed through TTSClient via speakWelcome() (fixes D-11 iOS bug) |
| Voice command confirmation — settings | "Opening settings." |
| Voice command confirmation — add allergy | "Opening settings." |
| Error state heading | (derived from message prop — existing ErrorState component, no change) |
| Error state body | (existing message + "Tap to try again" via retry button) |
| Retake guidance | (AI-generated from useMenuExtraction quality detection — existing, no change to copy) |
| Settings gear aria-label | "Open Settings — manage allergies and preferences" |

**Destructive actions this phase:** None. Clearing allergy profile is in the Settings page (Phase 5), outside this phase scope. "Scan New Menu" is NOT destructive (it dispatches RESET but does not delete data).

**Tone guidelines:** Reassuring, calm, restaurant-friendly. Not robotic. "Reading your menu..." not "Processing image data...". "Just a moment..." not "Please wait...".

---

## Voice Commands

**Source:** CONTEXT.md D-17; RESEARCH.md Pattern 5.

| Spoken phrase | Action | TTS confirmation |
|---------------|--------|-----------------|
| "open settings" / "go to settings" / "take me to settings" | `router.push('/settings')` | "Opening settings." |
| "add allergy" / "add allergen" / "I'm allergic" | `router.push('/settings')` | "Opening settings." |
| "scan new menu" / "new menu" / "scan again" | dispatch RESET → welcome state | "Starting over. Please take a new photo." |

Voice commands short-circuit before the chat API call in `useVoiceLoop.triggerResponse`. Commands are narrow-pattern regexps to avoid false positives (RESEARCH.md Pitfall 5 — require verb + noun phrase, not single words).

---

## Hint System (D-16, D-18)

**Source:** CONTEXT.md D-16, D-18; RESEARCH.md Pattern 6.

Session count tracked in IndexedDB `settings` store under key `'sessionCount'`. No DB version bump required.

| Session count | Behavior |
|---------------|----------|
| 1 (first visit) | Full spoken tutorial: "Welcome to MenuVoice! You can ask me about any item on the menu, get recommendations, or ask about ingredients. Just tap the microphone and speak naturally." Plays after the overview TTS. |
| 2–5 | Occasional contextual hint: 30% chance per conversation turn, max 1 hint per session. Example: "You can say 'open settings' to manage your food allergies." |
| 6–10 | Rare hint: 10% chance per turn, max 1 per session. |
| 11+ | Very rare: 5% chance per turn, max 1 per session. |

Hints are appended to the overview TTS in `useVoiceLoop` (not added to system prompt). Hint text tone: natural conversation, not tutorial-speak. "By the way, you can..." not "HINT: To navigate to...".

---

## Interaction Contracts by App State

| App State | Hero Action | Secondary Actions | Subdued/Hidden |
|-----------|-------------|-------------------|----------------|
| `welcome` | "Start" button (accent, 200px tall) | — | Header settings gear (visible but small) |
| `idle` | "Scan Menu" button (accent, full-width) | — | Header settings gear |
| `processing` | None (passive wait) | — | All buttons inactive |
| `results` | VoiceButton (accent, 96px circle) | "Scan New Menu" (muted, small) | Menu accordion (visible but below voice interface) |
| `retake` | "Scan Menu" (accent, full-width) | "Proceed with what I have" (muted, shown at 2+ attempts) | — |
| `error` | Retry button (accent) | — | — |

**Source:** CONTEXT.md D-04 (one hero action per screen).

---

## Accessibility Constraints

These are non-negotiable and carry forward from all prior phases:

1. All interactive elements: minimum 48px touch target height (44px minimum width for icon-only targets)
2. ARIA live regions (`AppStateAnnouncer`, `VoiceStateIndicator`, `ProcessingState` hidden div) are ALWAYS in DOM — never conditionally mounted or animated
3. Processing message ARIA region stays static — only the visible display cycles (prevents screen reader spam every 4 seconds)
4. Focus rings use `focus-visible:` not `focus:` — avoid showing rings on mouse taps
5. TTS output uses `<audio>` element only — never SpeechSynthesis (fixes two existing bugs: welcome TTS in handleStart + processing reminder interval)
6. `prefers-reduced-motion: reduce` disables all transitions/animations
7. Never put `aria-live` AND `role=alert` on the same element — `role=alert` implies assertive; double declaration causes VoiceOver iOS double-speak

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party | none | not applicable |

No registry components. All UI is custom Tailwind + React. No third-party component blocks.

---

## Files to Modify

Derived from codebase scan + design decisions. Executor reference:

| File | Change |
|------|--------|
| `src/app/globals.css` | Add `@theme` block with `--color-*` tokens + `prefers-reduced-motion` rule; replace `:root` hex vars with OKLCH |
| `src/app/layout.tsx` | Replace `bg-white text-black` with `bg-background text-foreground` on `<body>` |
| `src/components/Header.tsx` | Update background to `bg-background`; update logo teal; enlarge gear tap target; update aria-label |
| `src/components/ScanButton.tsx` | Add `variant` prop (`primary` | `secondary`); `primary` = accent style, `secondary` = muted subdued style; `secondary` also accepts custom label |
| `src/components/VoiceButton.tsx` | Replace `bg-black` with `bg-accent`; replace `ring-blue-600` with `ring-accent`; replace `focus-visible:outline-black` with `focus-visible:outline-accent` |
| `src/components/MenuSummary.tsx` | Update `bg-gray-50` → `bg-muted`, border → `border-muted`, dietary badges → `bg-accent/15 text-accent`, warning → `bg-warning-surface` |
| `src/components/TranscriptDisplay.tsx` | Update `bg-gray-50` → `bg-muted`, `text-gray-700` → `text-foreground`, `rounded-lg` → `rounded-xl` |
| `src/components/RetakeGuidance.tsx` | Update guidance panel to `bg-warning-surface`; update proceed button to muted style; fix TTS to use TTSClient not raw Audio |
| `src/components/ProcessingState.tsx` | Add cycling message logic with setInterval; visual display cycles, ARIA region stays static |
| `src/app/page.tsx` | Wrap state panels in FadePanel; fix welcome TTS to use speakWelcome() via TTSClient; fix processing interval to use TTSClient; add "Scan New Menu" label to secondary ScanButton; add voice command routing |
| `src/lib/tts-client.ts` | Add `fadeAndStop()` for D-10 interrupt; replace instant `audio.pause()` in `stop()` |
| `src/hooks/useVoiceLoop.ts` | Add voice command short-circuit; add hint logic with session count check |
| `src/lib/indexeddb.ts` | Add `getAndIncrementSessionCount()` function |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
