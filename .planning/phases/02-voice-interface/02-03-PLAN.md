---
phase: 02-voice-interface
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/VoiceButton.tsx
  - src/components/VoiceStateIndicator.tsx
  - src/components/TranscriptDisplay.tsx
  - src/components/MicPermissionPrompt.tsx
  - src/components/TextInputFallback.tsx
  - src/components/__tests__/VoiceButton.test.tsx
  - src/components/__tests__/VoiceStateIndicator.test.tsx
  - src/components/__tests__/TextInputFallback.test.tsx
autonomous: true
requirements: [A11Y-06]

must_haves:
  truths:
    - "Screen readers announce voice state changes via always-mounted ARIA live region"
    - "Microphone button has correct aria-label for each voice state"
    - "Firefox users see a text input fallback when Web Speech API is unavailable"
  artifacts:
    - path: "src/components/VoiceButton.tsx"
      provides: "80px circle mic button with state-driven appearance and aria-label"
      exports: ["VoiceButton"]
    - path: "src/components/VoiceStateIndicator.tsx"
      provides: "ARIA live region (role=status) + visible state label"
      exports: ["VoiceStateIndicator"]
    - path: "src/components/TranscriptDisplay.tsx"
      provides: "User utterance + streaming response text display"
      exports: ["TranscriptDisplay"]
    - path: "src/components/MicPermissionPrompt.tsx"
      provides: "Pre-browser-dialog accessible prompt"
      exports: ["MicPermissionPrompt"]
    - path: "src/components/TextInputFallback.tsx"
      provides: "Text input for browsers without Web Speech API"
      exports: ["TextInputFallback"]
    - path: "src/components/__tests__/VoiceStateIndicator.test.tsx"
      provides: "ARIA live region tests"
  key_links:
    - from: "src/components/VoiceStateIndicator.tsx"
      to: "screen reader"
      via: "role=status aria-live=polite aria-atomic=true always in DOM"
      pattern: "role=\"status\""
    - from: "src/components/VoiceButton.tsx"
      to: "src/lib/voice-state.ts"
      via: "VoiceState['status'] prop drives appearance and aria-label"
      pattern: "import.*VoiceState.*from.*voice-state"
---

<objective>
Build all five voice UI components specified in the UI-SPEC: VoiceButton, VoiceStateIndicator, TranscriptDisplay, MicPermissionPrompt, and TextInputFallback.

Purpose: These are the visual and accessibility primitives that useVoiceLoop (Plan 04) will render. Building them as standalone components with tests ensures they work correctly before integration.

Output: Five tested components matching the UI-SPEC visual and accessibility contracts, especially the A11Y-06 ARIA live region requirement.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-voice-interface/02-CONTEXT.md
@.planning/phases/02-voice-interface/02-UI-SPEC.md
@.planning/phases/02-voice-interface/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/voice-state.ts (created in Plan 01):
```typescript
export type VoiceState =
  | { status: 'idle' }
  | { status: 'listening'; transcript: string }
  | { status: 'processing'; transcript: string }
  | { status: 'speaking'; transcript: string; response: string }
  | { status: 'error'; message: string };
```

<!-- Existing Phase 1 component patterns -->

From src/components/ProcessingState.tsx (ARIA live region pattern):
```typescript
<div role="status" aria-live="polite" aria-atomic="true" className="sr-only">
  {isVisible ? message : ''}
</div>
```

From src/components/ScanButton.tsx (button pattern -- large touch target):
```typescript
// Phase 1 uses: min-h-14 (56px), bg-black text-white, rounded-2xl, text-lg font-semibold
// Phase 2 mic button: 80px circle per UI-SPEC
```

From src/components/ErrorState.tsx (role=alert pattern):
```typescript
<div role="alert" className="...">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceButton and VoiceStateIndicator components</name>
  <files>src/components/VoiceButton.tsx, src/components/VoiceStateIndicator.tsx, src/components/__tests__/VoiceButton.test.tsx, src/components/__tests__/VoiceStateIndicator.test.tsx</files>
  <read_first>
    - src/lib/voice-state.ts (VoiceState type -- the status field drives both components)
    - src/components/ProcessingState.tsx (ARIA live region pattern -- VoiceStateIndicator follows same pattern)
    - src/components/ScanButton.tsx (button styling patterns, focus-visible pattern)
    - src/components/__tests__/ProcessingState.test.tsx (test patterns for ARIA live region testing)
    - .planning/phases/02-voice-interface/02-UI-SPEC.md (Microphone Button section, Voice State Indicator section, Copywriting Contract, Accessibility Contract)
  </read_first>
  <action>
    **VoiceButton.tsx** -- `src/components/VoiceButton.tsx`

    Props:
    ```typescript
    interface VoiceButtonProps {
      status: VoiceState['status'];
      onTap: () => void;
      disabled?: boolean;
    }
    ```

    Renders an 80px circle button with inline SVG microphone icon (32px, `aria-hidden="true"`).

    Per UI-SPEC Microphone Button section, dynamic aria-label by status:
    - `idle`: "Tap to start listening"
    - `listening`: "Listening for your question. Tap to stop."
    - `processing`: "Thinking. Please wait."
    - `speaking`: "Speaking your answer. Tap to stop."
    - `error`: "Error occurred. Tap to try again."

    Visual states per UI-SPEC:
    - `idle`: `bg-black text-white` circle, `w-20 h-20 rounded-full` (80px)
    - `listening`: `bg-black text-white` circle + `ring-4 ring-blue-600 ring-offset-2 animate-pulse`
    - `processing`: `bg-gray-400 text-white` circle (disabled appearance)
    - `speaking`: `bg-gray-400 text-white` circle
    - `error`: `bg-gray-200 text-gray-600` circle

    Focus: `focus-visible:outline focus-visible:outline-4 focus-visible:outline-offset-2 focus-visible:outline-black`

    Button is disabled during `processing` state (set `disabled={true}`, `aria-disabled="true"`).

    Microphone SVG icon: use a simple path-based mic SVG inline. Example: `<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"><path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" x2="12" y1="19" y2="22"/></svg>`

    **VoiceStateIndicator.tsx** -- `src/components/VoiceStateIndicator.tsx`

    Props:
    ```typescript
    interface VoiceStateIndicatorProps {
      status: VoiceState['status'];
    }
    ```

    Contains TWO elements per A11Y-06 and UI-SPEC Voice State Indicator section:

    1. **ARIA live region** (screen reader): `<div role="status" aria-live="polite" aria-atomic="true" className="sr-only">`. ALWAYS in DOM (never conditionally mounted per Phase 1 ProcessingState pattern and UI-SPEC Accessibility Contract #1). Content text by status:
       - `idle`: "Tap to start listening"
       - `listening`: "Listening..."
       - `processing`: "Thinking..."
       - `speaking`: "Speaking..."
       - `error`: "Something went wrong -- tap to retry"

    2. **Visible label** (visual): `<p>` element below the mic button area. Same text as above. Styled `text-sm text-gray-600` (14px/400 per UI-SPEC Typography Status/State role). The visible label also shows the state-specific visual cue:
       - `listening`: Add blue-600 colored text
       - `processing`: Show existing Phase 1 spinner pattern (`w-5 h-5 border-2 border-black border-t-transparent rounded-full animate-spin`) inline
       - `speaking`: Show 3-dot bounce animation in green-600 (use CSS keyframes `@keyframes bounce` with staggered delays on 3 small circles)
       - `error`: Red-700 colored text

    **Tests:**

    `VoiceButton.test.tsx`: Test that aria-label matches for each status. Test onClick fires onTap. Test disabled during processing. Test correct background class per status (e.g., `bg-black` for idle, `bg-gray-400` for processing).

    `VoiceStateIndicator.test.tsx`: Test that `role="status"` element is ALWAYS in the DOM regardless of status. Test correct text content for each status value. Test that `aria-live="polite"` and `aria-atomic="true"` are present.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/VoiceButton.test.tsx src/components/__tests__/VoiceStateIndicator.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/VoiceButton.tsx contains `export function VoiceButton`
    - src/components/VoiceButton.tsx contains `aria-label` (dynamic)
    - src/components/VoiceButton.tsx contains `w-20 h-20` OR `w-20` AND `h-20` (80px)
    - src/components/VoiceButton.tsx contains `rounded-full`
    - src/components/VoiceButton.tsx contains `<svg` (inline mic icon)
    - src/components/VoiceButton.tsx contains `aria-hidden="true"` (on SVG)
    - src/components/VoiceStateIndicator.tsx contains `export function VoiceStateIndicator`
    - src/components/VoiceStateIndicator.tsx contains `role="status"`
    - src/components/VoiceStateIndicator.tsx contains `aria-live="polite"`
    - src/components/VoiceStateIndicator.tsx contains `aria-atomic="true"`
    - src/components/VoiceStateIndicator.tsx contains `sr-only` (screen reader only live region)
    - src/components/__tests__/VoiceStateIndicator.test.tsx contains test for role="status" always in DOM
    - npx vitest run src/components/__tests__/VoiceButton.test.tsx src/components/__tests__/VoiceStateIndicator.test.tsx exits 0
  </acceptance_criteria>
  <done>VoiceButton renders 80px circle with state-driven aria-labels and visual states. VoiceStateIndicator has always-mounted ARIA live region announcing state changes. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: TranscriptDisplay, MicPermissionPrompt, and TextInputFallback components</name>
  <files>src/components/TranscriptDisplay.tsx, src/components/MicPermissionPrompt.tsx, src/components/TextInputFallback.tsx, src/components/__tests__/TextInputFallback.test.tsx</files>
  <read_first>
    - src/lib/voice-state.ts (VoiceState type for TranscriptDisplay props)
    - .planning/phases/02-voice-interface/02-UI-SPEC.md (Transcript Display section, Mic Permission Pre-Prompt section, Text Input Fallback section, Copywriting Contract)
    - src/components/ErrorState.tsx (role=alert pattern for TextInputFallback warning)
    - src/components/ScanButton.tsx (button styling patterns for dismiss button and submit button)
  </read_first>
  <action>
    **TranscriptDisplay.tsx** -- `src/components/TranscriptDisplay.tsx`

    Props:
    ```typescript
    interface TranscriptDisplayProps {
      userTranscript: string;
      assistantResponse: string;
    }
    ```

    Per UI-SPEC Transcript Display section:
    - Container: `bg-gray-50 rounded-lg p-4`
    - `aria-live="off"` on the container (audio TTS carries the content, visual is supplementary)
    - If both userTranscript and assistantResponse are empty, render null (no empty container)
    - User utterance: prefix with `<span className="font-semibold">You:</span>` followed by transcript text. `text-gray-900`
    - Assistant response: `text-gray-700`, streams in as sentences are spoken
    - `max-h-48 overflow-y-auto` for scroll on long responses
    - 16px/400 (body) text size -- `text-base font-normal`

    **MicPermissionPrompt.tsx** -- `src/components/MicPermissionPrompt.tsx`

    Props:
    ```typescript
    interface MicPermissionPromptProps {
      onDismiss: () => void;
    }
    ```

    Per UI-SPEC Mic Permission Pre-Prompt section:
    - Container: `role="status"`, `bg-gray-50 border border-gray-200 rounded-lg p-4 text-base`
    - Copy: "MenuVoice needs your microphone to hear your questions. When prompted, tap Allow."
    - Dismiss button: "Got it, continue" -- `min-h-[48px] px-6 text-lg font-semibold bg-black text-white rounded-2xl`
    - Calls `onDismiss` when dismiss button clicked

    **TextInputFallback.tsx** -- `src/components/TextInputFallback.tsx`

    Props:
    ```typescript
    interface TextInputFallbackProps {
      onSubmit: (text: string) => void;
    }
    ```

    Per UI-SPEC Text Input Fallback section:
    - Warning message container: `role="alert"`, `bg-yellow-50 border border-yellow-200 rounded-lg p-4`
    - Warning text: "Voice recognition is not available in this browser. Try Chrome or Safari for the full experience."
    - Text input: `<input type="text">` with `min-h-[48px]` (meets touch target), `placeholder="Type your question..."`
    - Submit button: labeled "Send Question", same black button style as MicPermissionPrompt dismiss
    - On form submit: prevent default, call `onSubmit(inputValue)`, clear input
    - Use `<form>` wrapper so Enter key submits

    **Tests:**

    `TextInputFallback.test.tsx`: Test that role="alert" contains the Firefox fallback message. Test that submitting form calls onSubmit with input value. Test that input clears after submit. Test that empty input does not call onSubmit.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/TextInputFallback.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TranscriptDisplay.tsx contains `export function TranscriptDisplay`
    - src/components/TranscriptDisplay.tsx contains `aria-live="off"`
    - src/components/TranscriptDisplay.tsx contains `max-h-48 overflow-y-auto`
    - src/components/TranscriptDisplay.tsx contains `You:` (user transcript prefix)
    - src/components/MicPermissionPrompt.tsx contains `export function MicPermissionPrompt`
    - src/components/MicPermissionPrompt.tsx contains "MenuVoice needs your microphone"
    - src/components/MicPermissionPrompt.tsx contains "Got it, continue"
    - src/components/TextInputFallback.tsx contains `export function TextInputFallback`
    - src/components/TextInputFallback.tsx contains `role="alert"`
    - src/components/TextInputFallback.tsx contains "Voice recognition is not available"
    - src/components/TextInputFallback.tsx contains "Send Question"
    - src/components/__tests__/TextInputFallback.test.tsx contains at least 3 test cases
    - npx vitest run src/components/__tests__/TextInputFallback.test.tsx exits 0
  </acceptance_criteria>
  <done>TranscriptDisplay shows user/assistant text with proper aria-live=off. MicPermissionPrompt renders pre-dialog prompt with dismiss. TextInputFallback provides text input for Firefox with warning message. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass (Plans 01, 03, and existing Phase 1)
- VoiceStateIndicator has `role="status"` always in DOM (A11Y-06)
- VoiceButton has correct aria-label for all 5 states
- All components use exact copy from UI-SPEC Copywriting Contract
- Touch targets meet minimums (80px mic button, 48px other buttons)
</verification>

<success_criteria>
All five voice UI components render correctly per UI-SPEC. ARIA live region is always mounted. Aria-labels are dynamic and match copywriting contract. Visual states match color and spacing specs. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-interface/02-03-SUMMARY.md`
</output>
